# 全链路指挥中心 · 真实链路图

> **口径说明**：本文全部基于底层真实代码绘制（截至 2026-08-28，含门牌跟随安检、波次双保险、「已拒绝」归档、E 级清理等全部已合入修复）。
> 与早期设计流程图的区别：本文画的是**系统实际怎么跑**，不是设计意图。
>
> **图例**：🟪启动入口 ｜ 🔵处理环节 ｜ 🔶判定分流 ｜ 🟢成功终态 ｜ 🔴失败/拦截终态 ｜ ⬜看板/归档

---

## 一、总图：一个岗位的一生

```mermaid
flowchart TD
    M["🖱️ 手动即时模式<br/>POST /api/automation/trigger<br/>stop_at_review = False"]
    S["⏰ 每日定时调度 cron_time<br/>trigger_auto_apply_job<br/>stop_at_review = True"]

    P["🚀 run_full_auto_pipeline<br/>进程互斥锁 · 记录本轮 start_rowid"]

    M --> P
    S --> P
    P --> LOGIN["平台登录态预检<br/>失效平台本轮跳过"]
    LOGIN --> SCRAPE["🕷️ 平台抓取（预算制）<br/>关键词/城市/薪资队列轮巡<br/>rowid 差分窗口：只认本轮新增"]

    SCRAPE --> CLEAN{"🧹 双层清洗"}
    CLEAN -->|"Tier1 硬规则拦截<br/>Tier2 AI 排雷"| REJ["⬜ 淘汰岗位 Tab<br/>门牌=清洗淘汰/ai清洗淘汰<br/>（可用 /unreject 捞回重跑）"]
    CLEAN -->|"通过"| SYNC["📤 推送飞书多维表格<br/>公司+岗位+城市 三要素去重<br/>跟进状态=新线索"]

    SYNC --> EVAL["🤖 AI初评 evaluate_node<br/>8维加权打分 → 评级 A/B/C/D/F<br/>（Tavily情报+偏好底线+并发信号量）"]
    EVAL -->|"进LLM前结构化粗筛判垃圾"| REJ
    EVAL --> RT{"🔶 当前评级达精投门槛?<br/>（自动化阈值：A / B / C）"}

    RT -->|"达标·精投轨"| REW["✍️ rewrite_node 定制精投<br/>深度评估内联体检（8维诊断）<br/>定制简历+PDF+长图+专属话术<br/>回写飞书·门牌=简历人工复核"]
    RT -->|"未达·海投轨"| QG["💬 quick_greeting_node 海投装配<br/>通用话术（配置优先）+海投物料<br/>门牌预写跟随安检结果"]

    REW --> GATE
    QG --> GATE
    GATE{"🛡️ 安检闸门 evaluate_delivery_gate<br/>①海投岗公司规模 ≥ 1000 拦截<br/>②评级 ∈ 免审白名单 C/D/F?<br/>③平台 ∈ 自动投递白名单?"}

    GATE -->|"未过闸"| BP["⏸️ manual_review_node 断点挂起<br/>精投岗门牌=简历人工复核<br/>海投岗门牌=海投人工复核<br/>→ 待老板审批 Tab"]
    GATE -->|"过闸 + 手动模式"| DN["🚀 delivery_node 当场投递"]
    GATE -->|"过闸 + 定时模式"| RDY["📦 END 挂起图<br/>门牌=待投递 → 待投递 Tab"]

    BP -->|"放行 / 一键放行<br/>门牌→待投递 + boss_approvals放行标记"| RDY
    BP -->|"拒绝<br/>门牌→已拒绝（AI草稿全保留）"| REJM["⬜ 仅全部岗位列表归档<br/>rejected_manual（不进淘汰Tab）"]

    RDY --> FIRE{"🔶 发射方式"}
    FIRE -->|"「立即执行投递」按钮<br/>/deliver_approved"| ENG
    FIRE -->|"每日 10:00 / 14:30 双波次<br/>扫描待投递队列<br/>双保险：断点无放行标记→拦截回审"| ENG

    ENG["🖥️ 平台物料强校验网关 + DrissionPage<br/>BOSS必须有长图 / 智联猎聘51job必须有PDF<br/>全局串行锁·单窗口投递"]
    ENG -->|"成功"| OK["🟢 已投递 Tab<br/>飞书回写+投递时间戳"]
    ENG -->|"失败"| BAD["🔴 执行失败 Tab<br/>自动投递失败日志+推进建议<br/>重试=双轨拉起（重投递/重评估）"]
    DN --> OK
    DN --> BAD
```

---

## 二、子图 A：指挥中心前端结构与数据流

```mermaid
flowchart LR
    subgraph UI["前端 /prototype/command-center"]
        HDR["CommandHeader 顶栏<br/>手动/定时双模式切换<br/>今日Token统计（token_log表）<br/>基准简历/海投简历选择器"]
        FUNNEL["MissionFunnelCard 批次漏斗<br/>抓取→清洗→同步→评估→<br/>待审批→待投递→已投递"]
        RAIL["PipelineStepper 执行导轨·9模块<br/>角标数字=顺序位，完成变✅<br/>待审批>0常驻紫色高亮"]
        BOARD["LiveJobsBoard 岗位看板·7 Tab<br/>全部/淘汰/评估改写中/<br/>待老板审批/待投递/已投递/执行失败"]
        DRAWERS["StageConfigDrawer 配置抽屉<br/>抓取/清洗/飞书/初评/深度评估/<br/>简历改写/招呼语/待审批/自动投递"]
        TERM["TerminalDrawer 终端日志<br/>console-stream SSE·回放最近800行"]
        STORE["pipeline-store·zustand<br/>jobs字典=看板唯一数据源"]
    end

    SSE["SSE长连接 /api/tasks/logs<br/>stage/scrape/job/log/end 实时事件"]
    POLL6["6秒轮询<br/>current-pipeline + jobs-snapshot<br/>+run-snapshot（后端权威合并）"]
    POLL4["4秒轮询 /api/pipeline/config-status<br/>9模块已配置/待配置徽标"]

    SSE --> STORE
    POLL6 --> STORE
    STORE --> FUNNEL
    STORE --> RAIL
    STORE --> BOARD
    POLL4 --> RAIL
    DRAWERS -->|"保存各阶段规则"| BACKEND["后端 API 层"]
    HDR -->|"启动/trigger · 终止/abort<br/>定时/schedule-config热更新APScheduler<br/>简历/activate-resume · set-mass-apply-resume"| BACKEND
    BOARD -->|"放行/resume · 一键放行/resume_batch<br/>立即投递/deliver_approved · 自愈/auto-heal-and-approve<br/>放弃/dismiss-failed-job · 重试/retry-failed-job<br/>「定制」→跳沉浸工作台 /?job_id=xx"| BACKEND
    BACKEND --> SSE
    BACKEND --> POLL6
    BACKEND --> POLL4
```

**Tab 判定速查**（前端内存谓词过滤，计数=命中数）：

| Tab | 命中条件（status / node / 门牌） |
|---|---|
| 全部岗位 | 不过滤（rejected_manual 也只在这里出现） |
| 淘汰岗位 | 清洗淘汰 / ai清洗淘汰 / node=clean_rejected / rejected_auto（E/F 初评自动淘汰，node=clean_rule_rejected） |
| 评估/改写/投递中 | 其余 status=running（包含 AI 初评、深度评估、定向改写与自动投递执行中） |
| 待审批 | waiting / node=manual_review_node（门牌=简历人工复核·海投人工复核·待审批） |
| 待投递 | approved / ready_to_deliver / 门牌=待投递 |
| 已投递 | delivered / 门牌=已投递 |
| 执行失败 | error 或带 failure_info 且未复活 |

---

## 三、子图 B：数据摄取与清洗层

```mermaid
flowchart TD
    CFG["抓取配置抽屉<br/>关键词池 keywords（词+城市+薪资）<br/>平台开关与预算 platforms.limit"]
    CFG --> START["任务开工<br/>快照 MAX(rowid) → start_rowid<br/>（按任务切的增量窗口）"]
    START --> READY["ensure_platforms_ready<br/>登录态预检·失效平台跳过"]
    READY --> WORKER["每平台并发 worker<br/>找第一个未抓尽的条件→run_dispatch_collect<br/>rowid差分算inserted扣预算·抓尽换下一条件<br/>断点续抓页码=上次最后页+1"]
    WORKER --> DEDUP1["SQLite层去重指纹<br/>job_link 或 公司+岗位+城市<br/>INSERT OR IGNORE"]
    DEDUP1 --> SLEEP["随机礼貌延时<br/>BOSS 2~8秒·猎聘翻页30~60秒<br/>局部风控熔断：翻页异常/99错误码/无更多数据<br/>（PlatformGuard冷却与日限额已写好但主链路未接线）"]
    SLEEP --> RAW["raw_jobs 表入库<br/>process_status=已存入数据"]

    RAW --> T1{"Tier1 硬规则毫秒级预筛<br/>城市白名单/JD非空/学历/<br/>资历≤7年/薪资区间/关键词黑名单"}
    T1 -->|"命中"| REJ1["门牌=清洗淘汰<br/>淘汰岗位Tab"]
    T1 -->|"通过"| T2{"Tier2 AI侦察兵排雷<br/>小模型按must/never规则判JSON<br/>识别假招聘/外包坑"}
    T2 -->|"命中"| REJ2["门牌=ai清洗淘汰<br/>淘汰岗位Tab"]
    T2 -->|"通过"| FS["step2_sync_feishu<br/>公司+岗位+城市查重·已存在跳过<br/>跟进状态=新线索·state=已同步"]

    RAW2["手动重试/unreject 捞回<br/>单岗流水线独立入库"] -.->|不进入本轮增量| T1
```

---

## 四、子图 C：AI 初评与双轨物料

```mermaid
flowchart TD
    IN["岗位进入 evaluate_node<br/>注入：JD全景+基准简历+企业情报+偏好底线"]
    IN --> SF{"进LLM前 Python结构化粗筛<br/>is_obvious_garbage"}
    SF -->|"判垃圾"| FZ["直接评级F·门牌=清洗淘汰<br/>不消耗LLM"]
    SF -->|"正常"| LLM["大模型8维打分（temperature 0.1）<br/>role/skills/seniority/compensation/<br/>interview/company_stage/market/growth<br/>权重来自 evaluation_weights 表<br/>并发受 eval_concurrency 信号量约束<br/>情报开关 enable_company_search(Tavily)"]
    LLM --> SCORE["综合分 = 加权归一化100分制<br/>评级 A/B/C/D/F · 回写飞书"]

    SCORE --> TH{"评级达精投门槛?<br/>job_preferences表·自动化阈值"}
    TH -->|"A/B/C 达标·精投轨"| DE["深度评估内联体检（ai_scorer）<br/>仅注入JD+简历两块（刻意极简）<br/>产出7块结构化报告：JD分析/逐行审计/<br/>理想画像/能力词典/高杠杆点/毒点/行动计划<br/>回写飞书对应列"]
    DE --> RW["rewrite_node 定制改写<br/>rewrite_skill_id官方/自定义技能<br/>rewrite_include_diagnosis注诊断<br/>HTML渲染→PDF+长图<br/>/tmp临时渲染→传飞书附件→删本地<br/>写入打招呼语·门牌=简历人工复核"]
    TH -->|"未达·海投轨"| QG["quick_greeting_node<br/>mass_apply_greeting配置非空→直接用<br/>为空→现场用海投简历生成<br/>缺物料按需渲染挂载PDF+长图"]
    QG --> PRE{"门牌预写=安检预判<br/>evaluate_delivery_gate同源判定"}
    PRE -->|"过闸"| READY1["门牌=待投递"]
    PRE -->|"未过闸"| REVIEW1["门牌=海投人工复核"]
```

---

## 五、子图 D：安检、审批与状态机

```mermaid
flowchart TD
    GATE{"🛡️ evaluate_delivery_gate<br/>①精投轨跳过·海投岗公司规模第一个数字<br/>≥ mass_apply_max_headcount(默认1000)?<br/>②评级 ∈ auto_deliver_grades(C/D/F)?<br/>③平台 ∈ auto_deliver_platforms(4平台)?"}

    GATE -->|"任一不过"| BP["manual_review_node（图编译期 interrupt_before）<br/>精投岗门牌=简历人工复核<br/>海投岗门牌=海投人工复核<br/>→ 广播waiting→待老板审批Tab"]

    BP -->|"✅ 放行 /resume · 一键放行 /resume_batch<br/>门牌→待投递 + 登记boss_approvals放行标记"| RDY["待投递Tab<br/>物料与话术100%就绪"]
    BP -->|"❌ 拒绝<br/>门牌→已拒绝·AI草稿字段全保留"| REJ["rejected_manual<br/>仅全部岗位列表归档<br/>复活=重试重跑流水线"]

    RDY --> F1["方式1：立即执行投递<br/>/deliver_approved 双轨兜底：<br/>停在断点→Command(resume=True)续跑<br/>已结束存量→读飞书物料直调delivery_node"]
    RDY --> F2["方式2：定时双波次<br/>mass_deliver_time(10:00)/custom_deliver_time(14:30)<br/>扫待投递队列·平台白名单过滤"]

    F2 --> GUARD{"双保险：岗位是否停在审批断点<br/>且无 boss_approvals 放行标记?"}
    GUARD -->|"是（残留/泄露门牌）"| FIX["跳过发射·门牌回写海投人工复核<br/>等老板审后下轮再走"]
    GUARD -->|"否（真就绪）"| GO["进入投递执行"]

    F1 --> GO
```

**门牌 ↔ 看板 归属总表**（谁写入 → 落在哪个 Tab）：

| 跟进门牌 | 写入者 | 看板归属 |
|---|---|---|
| 新线索 | step2 飞书推送 | 全部岗位（评估中） |
| 清洗淘汰 / ai清洗淘汰 | Tier1 / Tier2 / 结构化粗筛 | **淘汰岗位** |
| 已完成初步评估 | 初评低分轨占位 | 全部岗位（done 卡） |
| 简历人工复核 | 精投轨评估完成 / manual_review_node | **待老板审批** |
| 海投人工复核 | manual_review_node / 门牌预写未过闸 / 波次纠错 | **待老板审批** |
| 待投递 | 过闸预写 / 放行动作 | **待投递** |
| 已投递 | 投递引擎成功回写 | **已投递** |
| 已拒绝 | 老板拒绝动作（新） | 仅全部岗位（rejected_manual） |
| 已下架 | BOSS 死链预检剔除 | 全部岗位 |
| 已放弃（SQLite） | 看板「放弃」dismiss | 移出指挥中心 |

---

## 六、子图 E：发射与投递执行

```mermaid
flowchart TD
    GO["获取待投递岗位"] --> LOCK["全局投递串行锁 _delivery_serial_lock<br/>同一时刻一个岗位占用浏览器<br/>单岗看门狗 delivery_timeout_sec(45s)"]
    LOCK --> MAT{"平台物料强校验网关"}
    MAT -->|"BOSS：缺「图片保存」长图"| BLOCKB["🔴 拦截报错：BOSS必须使用长图<br/>（delivery_node有缺料自愈装配+auto-heal接口兜底）"]
    MAT -->|"智联/猎聘/51job：缺「PDF备份」"| BLOCKP["🔴 拦截报错：缺少PDF简历附件"]
    MAT -->|"物料齐备"| ENG

    ENG["🖥️ DrissionPage 真实浏览器投递<br/>投前死链预检·「继续沟通」不重发打招呼"]
    ENG --> BOSS["BOSS：打开岗位→调起微聊<br/>→发长图简历+欢迎语<br/>单图30秒未送达重注×2<br/>部分送达→写待补发标记仍算成功<br/>resend_resume_images只补图"]
    ENG --> ZHILIAN["智联：附件简历上传投递<br/>+微聊欢迎语双动作<br/>searchId崩溃自动刷新重试"]
    ENG --> J51["51job：附件挂cvLogId+light-apply<br/>页面已投过→幂等视为成功"]
    ENG --> LIEPIN["猎聘：liepin 专属引擎"]

    BOSS --> OKC{"投递结果"}
    ZHILIAN --> OKC
    J51 --> OKC
    LIEPIN --> OKC
    OKC -->|"成功"| OK["🟢 飞书门牌=已投递<br/>记录投递时间戳·清空失败日志<br/>已投递Tab点亮"]
    OKC -->|"失败"| BAD["🔴 写自动投递失败日志<br/>（登录态失效/死链/缺料/searchId异常等）<br/>→ 执行失败Tab·带failure_info推进建议"]

    BAD --> RETRY{"重试（双轨分流）"}
    RETRY -->|"飞书岗（已进流程）"| RD["直接唤醒投递引擎重投<br/>/resume_job_delivery"]
    RETRY -->|"raw_岗（还在抓取/评估层）"| RE["重跑单岗LangGraph流水线<br/>run_single_job_pipeline_async<br/>record_id为空时跳过飞书查询<br/>用raw_rowid从SQLite补齐字段"]
```

---

## 七、已知的“形态差异”备忘（非缺陷，判定不改）

1. **增量窗口**：按任务起点 `MAX(rowid)` 切，非设计图的“T+3 时间窗”——效果等价（爬虫只在任务内跑，本轮号段=本轮新岗）；
2. **深度评估**：内联在 evaluate_node 高分轨，非独立节点；`deep_eval_node` 仅是广播别名叫得上号，指挥中心进度显示正常；
3. **平台保安**：PlatformGuard 冷却/日限额与 PlatformSemaphore 已写好但主链路未接线，实际靠随机 sleep + 各爬虫局部熔断；出现风控迹象时再移植；
4. **审批唤醒**：approve 只盖飞书门牌，图解冻延后到投递按钮/波次的 `Command(resume=True)`（裸命令，物料已在档案）——即四格规格的两段式。
